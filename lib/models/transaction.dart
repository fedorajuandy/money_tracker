class Transaction {
  String name;
  String type;
  String category;
  double amount;
  DateTime transactionMade;

  Transaction(this.name, this.type, this.category, this.amount, this.transactionMade);
}
